1) Make sure all datatypes are handled in read_datatype
2) implement read_datavector for all datatypes -> learn how to make this generic possibly struct DataTypeVec<T>(Vec<T>)
3) correctly handle segment adress 0xFFFF_FFFF in map_segments (edge cases related to malformed files?)
4) Implement handling of interleaved data
5) Make the "new object is appended to existing objects in segment" case within read_metadata more efficient
6) Handle DaqMX datattypes
7) Bring error handling up to modern standard and thread into app
DONE 8) Get plotting of data to work for visual debugging!
9) The load_data function requiring a mutable file ref feels wrong.
10) cache loaded data.
